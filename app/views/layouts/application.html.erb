<!DOCTYPE html>
<html>
  <head>
    <title>FirstBank</title>
    <%= csrf_meta_tags %>

  </head>

  <body>
    <!-- render 'layouts/navbar -->
    <div class='container'>
      <% flash.each do |key, value| %>
        <div class="alert alert-<%= key %>">
          <%= value %>
        </div>
      <% end %>
    </div>
    <%= yield %>
  </body>
</html>